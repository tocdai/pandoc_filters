# This repo contains pandoc filters to manipulate the document convertion using pandoc
- `headers-only`: only keep headers
- `no-images`: remove all images tag

About `pandocfilters`: https://github.com/jgm/pandocfilters